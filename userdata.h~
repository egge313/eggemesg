#ifndef USERDATA_H
#define USERDATA_H 1

#include <QString>

class UserData
{
public:
  QString m_user;
  QString m_password;

  UserData (QString user, QString password) : 
    m_user(user), m_password(password) {} 
  ~UserData () {}
};

#endif
