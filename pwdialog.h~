#ifndef PWDIALOG_H
#define PWDIALOG_H

#include <QDialog>
#include "userdata.h"

namespace Ui {
class PwDialog;
}

class PwDialog : public QDialog
{
    Q_OBJECT

public:
    explicit PwDialog(QWidget *parent = 0);
    ~PwDialog();

public slots:
      void accept ();
      void reject ();

      //signals:
      // void setUserSuccessfully(UserData * userdata);

private:
    Ui::PwDialog *ui;
};

#endif // PWDIALOG_H
